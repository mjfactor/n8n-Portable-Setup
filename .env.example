# =============================================================================
# SUPABASE POSTGRESQL DATABASE CONFIGURATION
# =============================================================================
# Get these values from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/database

# Database host (Session mode - port 5432, recommended for n8n)
# Format: aws-0-REGION.pooler.supabase.com
SUPABASE_DB_HOST=aws-0-ap-southeast-1.pooler.supabase.com

# Database user (includes your project reference)
# Format: postgres.YOUR_PROJECT_REF
SUPABASE_DB_USER=postgres.your_project_ref_here

# Database password (get from Supabase dashboard)
SUPABASE_DB_PASSWORD=your_secure_database_password_here

# =============================================================================
# N8N SECURITY CONFIGURATION
# =============================================================================

# REQUIRED: Encryption key for credentials and sensitive data
# Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('base64'))"
# IMPORTANT: Keep this key safe! Losing it means losing access to encrypted credentials
N8N_ENCRYPTION_KEY=your_24_character_base64_encryption_key

# =============================================================================
# USEFUL COMMANDS
# =============================================================================
# Start n8n:           docker-compose up -d
# View logs:           docker-compose logs -f n8n
# Stop n8n:            docker-compose down
# Restart n8n:         docker-compose restart n8n
# Update n8n:          docker-compose pull && docker-compose up -d
# Backup data:         ./backup_n8n.sh (or .bat on Windows)
